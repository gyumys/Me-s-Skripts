command /아이템보기 [<text>]: #아이템들이 배열을 gui로 쉽게 볼수 있게 해주는걸 만들려했지만 편의상 랜무무기만 함.
	trigger:
		set {_gui} to chest inventory with 6 rows named "아이템"
		add {game::rww::item::*} to {_gui}
		open {_gui} to player